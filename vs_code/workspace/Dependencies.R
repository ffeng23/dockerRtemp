#this is the one to include the extra libraries in order to
# fill the vs-code requirement

library(yaml)

library(jsonlite)

library(knitr)

library(rmarkdown)

library(here)

#library(languageserver)

library(reticulate)